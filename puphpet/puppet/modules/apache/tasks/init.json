{
  "description": "Allows you to perform apache service functions",
  "input_method": "stdin",
  "parameters": {
    "action": {
      "description": "Action to perform ",
      "type": "Enum[reload]"
    }
  }
}
