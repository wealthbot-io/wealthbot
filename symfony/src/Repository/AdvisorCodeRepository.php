<?php

namespace App\Repository;

use Doctrine\ORM\EntityRepository;

/**
 * AdvisorCodeRepository.
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class AdvisorCodeRepository extends EntityRepository
{
    /**
     * @param null $name
     *
     * @return array
     */
    public function getAll($name = null)
    {
        $qb = $this->createQueryBuilder('code');

        if (!empty($name)) {
            $qb->where('code.name LIKE :name')->setParameter('name', "%{$name}%");
        }

        return $qb
            ->orderBy('code.name', 'ASC')
            ->getQuery()
            ->getResult()
        ;
    }
}
