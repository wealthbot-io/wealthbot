{% extends '/Mailer/Client/_client_layout.html.twig' %}

{% block subject %}
    {% autoescape false %}
        {{ 'Continue Your Registration' }}
    {% endautoescape %}
{% endblock %}

{% block message %}
    {% if (ria.riaCompanyInformation.slug) %}
        {% set login_url = subdomain_url(ria.riaCompanyInformation, 'login') %}
    {% else %}
        {% set login_url = url('login') %}
    {% endif %}

    {{ client.firstName }},<br/><br/>
    We noticed you haven’t completed your registration on our system yet. Please <a href="{{ login_url }}">
    continue your registration</a> so we can help you reach your investment goals.
{% endblock %}