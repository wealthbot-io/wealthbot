{% set index_route           = index_route is defined  ? index_route : 'rx_ria_categories_index' %}
{% set asset_delete_route    = index_route is defined  ? index_route : 'rx_ria_categories_asset_delete' %}
{% set subclass_delete_route = index_route is defined  ? index_route : 'rx_ria_categories_subclass_delete' %}

{#% include '/Admin/Categories/index.html.twig' %#}